// Adding a new dashboard page to display user repos and commands
// This requires further implementation to fetch and display the data

// Placeholder route for the new dashboard
app.get("/dashboard", (req, res) => {
  res.send("<h1>User Dashboard - (PlaceHolder)</h1><p>Displaying user repos and commands...</p>");
});

// Existing code remains unchanged below
